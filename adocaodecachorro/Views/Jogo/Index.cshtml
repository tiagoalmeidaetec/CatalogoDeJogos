@model IEnumerable<CatalogoDeJogos.Models.JogoModel>
@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<a asp-action="Create">Create New</a>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GameID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Genero)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AnoLancamento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GOT)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.GameID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Genero)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.AnoLancamento)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.GOT)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */}) |
                    @Html.ActionLink("Details", "Details", new { id = ((List<CatalogoDeJogos.Models.JogoModel>)Model).IndexOf(item) }) |
                    @Html.ActionLink("Delete", "Delete", new {id = ((List<CatalogoDeJogos.Models.JogoModel>)Model).IndexOf(item) })
                </td>
            </tr>
        }
    </tbody>
</table>

